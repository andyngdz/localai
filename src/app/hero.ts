import { heroui } from '@heroui/react'

// HeroUI Tailwind plugin configuration
export default heroui({
  themes: {
    dark: {
      colors: {
        primary: '#f77f00',
        background: '#121212'
      }
    }
  }
})
